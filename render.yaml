services:
  - type: web
    name: career-guidance-system
    runtime: node
    plan: free
    autoDeploy: true
    buildCommand: corepack enable; pnpm install --frozen-lockfile; pnpm prisma generate; pnpm test; pnpm build
    startCommand: pnpm deploy:checks; pnpm start
    healthCheckPath: /
    envVars:
      - key: NODE_VERSION
        value: 22
      - key: NODE_ENV
        value: production
      - key: NEXT_TELEMETRY_DISABLED
        value: "1"
      - key: DATABASE_URL
        sync: false